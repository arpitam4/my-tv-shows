// import { RootState } from '../store';

// export const selectShowDetails = (state: RootState) => state.shows.details;
// export const selectShowLoading = (state: RootState) => state.shows.loading;
// export const selectShowError = (state: RootState) => state.shows.error;
// export const selectShowCast = (state: RootState) => state.shows.cast;